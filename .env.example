# Netlify automatically provides NETLIFY_DATABASE_URL when you connect Neon through Netlify DB
# For local development, you can still use DATABASE_URL
DATABASE_URL="postgresql://[user]:[password]@[host]/[database]?sslmode=require"

# Note: When deployed to Netlify, use NETLIFY_DATABASE_URL which is automatically configured
